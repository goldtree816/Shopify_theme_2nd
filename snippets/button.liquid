{% if link %}
  <a href="{{ link }}" class="btn-primary {{ class }}">
    {{ text }}
  </a>
{% else %}
  <button
    type="{{ type | default: 'button' }}"
    class="btn-primary {{ class }}"
  >
    {{ text }}
  </button>
{% endif %}
